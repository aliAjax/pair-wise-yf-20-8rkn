# hxyfront-62008 烟花燃放脚本编排

源提示词编号：10

我想做一个面向烟花燃放编排师的燃放脚本前端工具，可以记录节目段落、烟花型号、口径、发射角度、点火时间、持续时间、安全距离和音乐时间点。页面需要有时间轴编排、燃放点位平面图、型号清单、冲突时间提示和整场节目预览。

## 技术栈

React + Vite + TypeScript

## 放行核算

编排页已接入安保疏散核算，判定、存储、页面分三个业务文件：

- `src/judgment.ts`（判定）：纯函数核算。离场口按宽度每分钟每米通行 120 人，疏散完成后另加 120 秒清场缓冲；每段疏散自上一段燃放结束开始，清场结束晚于计划点火即阻塞，并给出最早可点火时刻；阻塞会级联顺延下游段落。
- `src/storage.ts`（存储）：预置三段节目、三个离场口、三区人数；数据读写浏览器 localStorage，含结构校验与损坏回退。
- `src/App.tsx`（页面）：预警区与时间轴展示阻塞原因和最早可点火时刻；离场口关停/调宽、区域人数调整会让相关段落失效重算。

## 本地运行

```bash
npm install
npm run dev
```

开发端口：62008
